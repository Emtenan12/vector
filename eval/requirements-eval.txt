# Evaluation-only dependencies for the extraction/embedding bake-off.
# NOT the production pipeline's requirements -- see pipeline/requirements.txt,
# which only pins whichever tools the evaluation actually justified keeping.
pymupdf4llm==1.28.0
markitdown==0.1.6
docling==2.115.0
pypdf==6.14.2
sentence-transformers==5.6.1
rank-bm25==0.2.2
chromadb==1.5.9
